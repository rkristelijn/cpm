# cpm tools

Show installed tool versions.

## Usage

```bash
cpm tools
```

## Output

Lists all tools from `cpm.toml [tools]` section with their installed version and status (installed/missing/outdated).

## See also

- `cpm install` — install missing tools
- `cpm audit` — check version compliance
