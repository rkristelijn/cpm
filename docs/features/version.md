# cpm version

Show the current project version.

## Usage

```bash
cpm version        # show version
cpm bump patch     # increment patch (0.1.0 → 0.1.1)
```

## See also

- `cpm bump` — increment version
