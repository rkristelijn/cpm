# cpm audit

Check installed tool versions against `cpm.toml` requirements.

## Usage

```bash
cpm audit
```

## Output

Reports which tools are installed, their version, and whether they match the pinned version in `cpm.toml`.

## See also

- `cpm install` — install missing tools
- `cpm tools` — show all installed versions
