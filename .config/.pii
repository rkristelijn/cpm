# .pii — Paths and patterns that should not appear in committed code.
# Used by check-pii.sh to detect accidental references to private/work systems.
#
# Format: one pattern per line (grep -E compatible)

# Work paths
~/git/lab/
/git/lab/
theapsgroup
atlassian.net
clickup.com/t/

# Personal identifiers
@theapsgroup
